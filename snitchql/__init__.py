"""SnitchQL - a DBISAM table explorer, query engine, and index tool.
Read layer uses the official DBISAM ODBC driver (native on Windows);
a pure-python fallback parser exists for offline/dev use.
"""
__version__ = "0.1.0"
